// Generated by CoffeeScript 1.10.0
(function() {
  window.Util = {};

  Util.Vector2 = function(x, y) {
    this.X = x;
    this.Y = y;
    this.add = function(vector) {
      return new Util.Vector2(this.X + vector.X, this.Y + vector.Y);
    };
    this.multiply = function(scalar) {
      return new Util.Vector2(this.X * scalar, this.Y * scalar);
    };
    return this;
  };

  Util.ticksToMilliseconds = function(d) {
    return 1000 / d;
  };

  Util.UUID = function(a) {
    if (a) {
      return (a ^ Math.random() * 16 >> a / 4).toString(16);
    } else {
      return ([1e7] + -1e3 + -4e3 + -8e3 + -1e11).replace(/[018]/g, Util.UUID);
    }
  };

  Util.sizeCanvas = function() {
    var canvas;
    canvas = $('#screen')[0];
    canvas.style.paddingTop = $('#navbar').height();
    canvas.style.width = window.innerWidth;
    canvas.style.height = window.innerHeight - $('#navbar').height() * 2 - $('.controls').height() * 1.5;
    canvas.width = canvas.offsetWidth;
    canvas.height = canvas.offsetHeight - $('#navbar').height();
    return new Util.Vector2(canvas.width, canvas.height);
  };

  Util.clear = function() {
    var canvas, canvasContext;
    canvas = $('#screen')[0];
    canvasContext = canvas.getContext('2d');
    return canvasContext.clearRect(0, 0, canvas.width, canvas.height);
  };

}).call(this);
